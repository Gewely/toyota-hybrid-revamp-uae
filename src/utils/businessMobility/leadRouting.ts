import { BusinessBuilderState, LeadScore } from './types';
export function routeLead(state: BusinessBuilderState, leadScore: LeadScore): string { const models = state.vehicles.map(v=>v.model); if (leadScore === 'Hot') return 'Priority manager queue'; if (state.businessType === 'Government') return 'Key account/fleet team'; if (models.some(m=>['Hiace','Coaster'].includes(m))) return 'Commercial/fleet specialist'; if (state.vehicles.some(v=>v.category==='Commercial')) return 'Commercial sales specialist'; if (state.ownership === 'Drive-on-Lease') return 'Drive-on-Lease team'; if (state.vehicleCount === 1 && state.businessType === 'Personal business owner') return 'Retail sales team'; if (state.vehicleCount >= 6) return 'Fleet sales team'; if (state.vehicleCount >= 2) return 'SME business advisor'; return 'Retail sales team'; }
