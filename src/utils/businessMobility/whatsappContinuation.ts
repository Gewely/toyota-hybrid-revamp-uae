import { BusinessBuilderState, CostEstimate, LeadFormData } from './types';
const TODO_TOYOTA_WHATSAPP_NUMBER = '971000000000'; // TODO: replace with NEXT_PUBLIC_TOYOTA_WHATSAPP_NUMBER or existing Toyota WhatsApp config.
export function getToyotaWhatsAppNumber(): string { return import.meta.env.VITE_TOYOTA_WHATSAPP_NUMBER || import.meta.env.NEXT_PUBLIC_TOYOTA_WHATSAPP_NUMBER || TODO_TOYOTA_WHATSAPP_NUMBER; }
export function buildWhatsAppContinuationUrl(state: BusinessBuilderState, lead: Partial<LeadFormData>, estimate: CostEstimate): string { const vehicleCount = state.vehicles.reduce((s,v)=>s+v.quantity,0); const text = `Hi Toyota, I submitted a Business Mobility Builder request.\n\nCompany: ${lead.companyName || 'Not provided'}\nUse case: ${state.useCase}\nVehicles: ${vehicleCount}\nOwnership preference: ${state.ownership}\nEstimated monthly cost: AED ${estimate.totalMonthlyCost.toLocaleString()}\nPreferred contact: ${lead.preferredContactMethod || 'WhatsApp'}\n\nPlease continue my business mobility proposal.`; return `https://wa.me/${getToyotaWhatsAppNumber()}?text=${encodeURIComponent(text)}`; }
